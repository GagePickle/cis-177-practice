const name = "Gage"; // constant
let numOfYearsCoding = 9;
// Accessing an OBJECT called 'console' and using DOT NOTATION to access a KEY .
// That KEY is a METHOD, so we INVOKE it with ().
// We also pass in an ARGUMENT as a STRING.
// ARGUMENTS provide additional information that a METHOD needs to do its job.
console.log("Hello World!");
console.log(); // not an error but does not do anything because no argument
